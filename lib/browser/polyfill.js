"use strict";
// eslint-disable-next-line no-undef
(globalThis || window).process = require('../../shims/process');
// eslint-disable-next-line no-undef
(globalThis || window).Buffer = require('buffer').Buffer;
